export default function Activity(props) {
  // const userItem = props.users.map((user) => (
  //   // <div>
  //   //   key={user.id} name={user.first_name} spots={user.last_name}
  //   // </div>
  // ));
  // return <ul>{userItem}</ul>;
  return <></>;
}
